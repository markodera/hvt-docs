# Optional. Defaults to the checked-in OpenAPI schema.
VITE_API_SCHEMA_URL=/schema.yaml

# Optional. Used in example snippets rendered in the docs UI.
VITE_API_URL=https://api.hvts.app

# Optional. Used in example snippets rendered in the docs UI.
VITE_HVT_API_KEY=hvt_test_your_api_key
